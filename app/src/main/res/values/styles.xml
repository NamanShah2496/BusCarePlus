<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/menu_blue</item>
        <item name="colorPrimaryDark">@color/menu_blue</item>
        <item name="colorAccent">@color/menu_blue</item>

    </style>

    <style name="SplashScreen" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimaryDark">@color/menu_blue</item>
        <item name="android:windowBackground">@drawable/splashscreen_background</item>


    </style>

    <!-- dark application theme. -->
    <style name="darkTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorAccent">@color/maroon</item>

        <item name="android:background">@color/black</item>
        <item name="cardBackgroundColor">@color/black</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:colorButtonNormal">@color/darkgrey</item>
    </style>

</resources>